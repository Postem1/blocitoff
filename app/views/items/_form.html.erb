<h2>Add an item</h2>
<%= form_for [user, item] do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control', placeholder: "Enter a new to-do item" %>
  </div>
  <%= f.submit "Submit Item", class: 'btn btn-success' %>
<% end %>
